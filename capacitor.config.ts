import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.appversuchschwanger',
  appName: 'AppVersuchSchwanger',
  webDir: 'build'
};

export default config;
